# CLAUDE.md

ohm-lens — ハードウェア・電子工作プロジェクトのリポジトリ。ドキュメント中心の
構成で、設計メモと回路図を管理する。

## このリポジトリの構造

- `README.md` — プロジェクト概要（現時点ではプレースホルダ）
- `docs/` — 設計メモ、検討事項、部品選定、測定結果
- `diagrams/` — PlantUML ソースと生成 PNG
- `CHANGELOG.md` — バージョンごとの変更履歴

## 編集時の注意

- PlantUML 編集後は `cd diagrams && plantuml *.puml` で PNG を再生成する
- 図とテキストの説明は意味が一致するように両方を更新する
- Markdown を編集したら `npx markdownlint-cli '**/*.md'` を通してエラーをゼロにする
- 部品の型番・定数（抵抗値、電圧など）を変更したら、README・docs・図の
  すべての言及箇所を追従更新する

## やってはいけないこと

- `git push` を勝手に実行しない（ユーザが明示的に指示したときのみ）
- 設計の意味を変える編集を黙って行わない（提案 → 確認 → 適用の手順）
- `*-ignore.md`（ローカル専用メモ）を git 管理下に入れない
