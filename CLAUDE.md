# CLAUDE.md

OhmLens — Web カメラに映した抵抗器の抵抗値をリアルタイムでオーバーレイ表示する
Web アプリ。**ブラウザ完結（クライアントサイド推論）でサーバを持たない。**

## このリポジトリの構造

- `README.md` — プロジェクト概要とロードマップ
- `docs/01-設計.md` — 設計の一次情報。アーキテクチャ・ライブラリ選定・
  ホスティング要件・ネーミング調査はここが正
- `diagrams/` — PlantUML ソースと生成 PNG
- `CHANGELOG.md` — 変更履歴

## 設計上の前提（変更するなら必ず確認を取る）

- **2 段パイプライン**: NN で位置検出 → 古典 CV で色帯読み取り。値の算出は
  決定的アルゴリズムで行う。カラーバンド読み取りまで NN に任せない
- **物体検出は OBB（回転バウンディングボックス）**。軸平行ボックスでは隣の
  抵抗器やリード線を巻き込んで色帯解析が破綻する
- **色分類は Lab + ΔE2000 の相対分類**。HSV だけでは茶／赤／橙、金／黄が
  分離できない。絶対 Lab 値ではなく、フレーム内の基準色（抵抗器の本体色、
  UI ガイド枠内の白基準）をアンカーにした相対分類を主防御にする
  （WB ロックは Safari で不可能なため）
- **OpenCV.js は Phase 0 限定の暫定依存**。Phase 1 以降は回転切り出しを
  OffscreenCanvas 2D で代替し、依存から外す
- **色帯解析は元解像度の ROI で行う**。推論用の 640px ダウンスケール画像から
  切り出さない
- **Phase 0（静止画・OpenCV.js のみ）を飛ばさない**。最初から YOLO を入れると
  検出と色判定のどちらが悪いか切り分けられなくなる

## 実装時の注意

- `whiteBalanceMode` / `exposureMode` の `manual` 固定は **Chromium 限定の
  最適化**として扱う（Safari は未対応）。固定できる環境では併用する
- フレーム同期は `video.requestVideoFrameCallback()` を使う
  （`setInterval` / `requestAnimationFrame` は使わない）
- 映像座標と Canvas 座標の変換関数は **一箇所にまとめる**。`object-fit: cover`
  で内在解像度と CSS 表示サイズがズレる
- cross-origin isolation（COOP/COEP）を張る前提のため、**外部 CDN に依存せず
  アセットは自ホストに置く**
- 確信度が閾値未満のときは値を出さず「?」を表示する。誤った値を自信ありげに
  出さない

## 作業ルール

- Markdown を編集したら `npx markdownlint-cli '**/*.md'` を通してエラーをゼロにする
- PlantUML 編集後は `cd diagrams && plantuml *.puml` で PNG を再生成する
- 設計を変更したら `docs/01-設計.md` と `README.md` の両方を追従更新する

## やってはいけないこと

- `git push` を勝手に実行しない（ユーザが明示的に指示したときのみ）
- 設計の意味を変える編集を黙って行わない（提案 → 確認 → 適用の手順）
- `*-ignore.md`（ローカル専用メモ）を git 管理下に入れない
- 学習済みモデル・撮影した学習データを無断でコミットしない（サイズと権利の問題）
