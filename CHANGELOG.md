# Changelog

このプロジェクトの変更履歴。

## [Unreleased]

### 効かなかった: HiRes 論文の 2 施策

- 抵抗器カラーコード読み取りの最新手法
  [HiRes](https://arxiv.org/abs/2606.30179)（2026-06）から、NN に依存しない
  2 施策の移植を計画し、着手前の計測で**どちらも否定した**
- **ギャップ保存分離** — 論文のアブレーションでは外すと -34.9% だが、
  OhmLens では前提の現象（同色隣接バンドの融合）が **39 枚で 0 件**。
  OhmLens は先にランを切ってから分類するので平滑の段が無く、埋まる
  ギャップも無い
- **相対幅フィルタ**（幅中央値の 20% 未満を捨てる）— **39 枚すべてで 0 本**
  しか落とさない。過分割で増えるランは本物のバンドと同じ幅を持つ
- **E24 系列検証**は論文のアブレーションでも寄与 0%。`core/value/eseries.ts`
  に伸びしろが無いことの裏付け
- 副産物として保留 12 枚の内訳が分かった（分類の誤り 5 / 検出不足 3 /
  確信度不足 2 / 過分割 2）。詳細は
  `docs/12-HiRes論文の施策を試した結果.md`。切り分けには
  `scripts/dbgGap.ts` を追加した

### 見つからなかった許容差バンドを位置を絞って拾い直す

- 実測では金・銀の許容差バンドが規則的に消える（人手ラベルのある 35 枚のうち
  **31 枚が「ラン 3 本 / バンド 4 本」**で不足はきっかり 1 本）。全体の `edgeΔE` を
  下げて拾おうとすると他の写真で陰影を拾って過分割になる
- **どこを探すか分かっていれば、全体より弱い基準を使ってよい。**
  3 本しか出ず、3 本目が本体の 40〜60% にあるとき、1→3 の方向へバンド間隔の
  1〜2 倍の窓を作り、本体色との差が 9（全体の分割は 20）を超える最長区間を
  4 本目として拾う（`core/bands/recover.ts`）
- **14 枚で発火**。3 ランの写真が 27 → 13 枚に減った
- **値の成績は 27/0/12 で不変**。4 本帯の 3 ラン状態はもともと全部正解で、
  許容差バンドは抵抗値に影響しないため。上がったのは表示できる情報の量で、
  許容差の色は 14 枚中 **9 枚が正解**（誤りは 5 枚とも 金→銀）
- しきい値を下げると発火は増えるが**値の成績は落ちる**
  （ΔE 5 で 4 ラン 30 枚・正解 26、ΔE 9 で 17 枚・正解 27）。9 が上限
- 拾えなかった 13 枚は**すべて 40〜60% の条件内**で、窓のなかに閾値を超える
  色変化が無いのが理由。金が本体に溶けきっている
- 較正側は `keepEdgeRuns: true` でランが 3 本を超えるため発火しない
- **効かなかった: 位置からの推測の一般化。** 検出済みの位置から格子（バンドの
  刻み）を推定し、空いている位置を内挿・外挿して探る形に広げると悪化する
  （内挿＋外挿で正解 25、内挿のみ最大 1 本でも 26。採用中の形は 27）。
  **許容差バンドは末尾に足すだけなので値が変わらないが、格子の内挿は
  数字・倍率バンドを挿入するので値が直接変わる。** 偽陽性が 1 本入ると桁がずれる。
  拾い直しが安全なのは「足しても値が変わらない位置」に限られる

### 許容差バンドの間隔を方向判定の根拠に足した（弱い項として）

- IEC 60062 は許容差バンドを他より離して印刷する。実測でも `15-339ohm` は
  間隔が 3 / 3 / 4 / **14** と許容差の手前だけ 3.5 倍開く
- ただし**単独では当てにならない**。`docs/07` では「許容差バンドの間隔で方向を
  決める」を試して 14 枚中 4 枚しか合わず 4 枚は逆だった。今回ラベルが揃ったので
  測り直したが、突出した間隔を持つ 4 枚（15 / 01 / 07 / 09）のうち正しいのは
  **15 の 1 枚だけ**で、残り 3 枚の広い間隔は検出の乱れ（欠損・過分割）由来だった
- そこで**方向を決める根拠にはせず、`jointDecode` のコストに弱く足す**形にした。
  色・E 系列・流通範囲の根拠が拮抗したときだけ効く。
  3 バンドの読みには効かせない（許容差バンドが無いため）
- **読み取りの成績は変わらない（27/0/12）。** 項が発火する 4 枚はいずれも別の
  理由で保留のため、効果も悪化も測れていない。
  `15` は方向ではなく色の誤読（白→銀・黒→赤）が原因で、README のとおり
  CG イラストなので色較正には使えない画像だった
- 効かせてはいけない場面で黙ることをテストで担保した（3 バンドでは無視、
  間隔が均一なら読みを変えない）

### 中心線プロファイルのグラフを焼き込むようにした

- バンド一覧表の右に、**ラン分割が実際に食べている値**（色順応補正後の
  L\* / a\* / b\*）を 1 枚のグラフとして描く。X 軸は抵抗器の長さ方向で、
  表の番号と並びが対応する
- **赤い縦線がラン境界**、両端の灰色が本体範囲の外。「なぜこの本数に
  なったか」を目で追える。バンドがあるのに境界が引かれていない箇所は
  そのまま取りこぼしとして見える
- 各ランの上に**通し番号**を重ねた。写真・一覧表と同じ番号なので、
  グラフの山谷とバンドの色名を突き合わせられる。除外されたランは薄く描く
- **L\* と a\*/b\* でスケールを分けた**（値域が 0〜130 と −60〜80 で桁が違い、
  同じ軸だと L\* の変化ばかりが目立っていた）。目盛りは左が L\*、右が a\*/b\* で、
  a\* と b\* は 1 つのスケールを共有する。どちらも最大値がプロット高の 90%、
  最小値が 10% に来るよう自動で合わせる
- 検出は正しいのにランが足りない、という今日の主要な症状
  （`docs/11`）を 1 枚の画像で確認できるようにするのが狙い。
  実際 `35-1Mohm` では、金バンドの位置に L\* の落ち込みがあるのに
  ラン境界が引かれていないことが見て取れる

### 焼き込み画像のバンド表示を表形式にした

- **写真に重ねるのは通し番号だけ**にした。色名・色玉・意味は写真の下の
  一覧表にまとめる
- 表は 1 列 1 バンドで 4 行（番号 / 色名 / 色玉 / カラーコード上の意味）。
  1 行目の番号は写真に焼いた番号と対応する
- 以前は色名と意味をバンドのすぐ脇に置いていたが、バンドが詰まると重なるので
  1 本ごとに段違いにする必要があり、それでも読みにくかった。
  段違いのぶん注釈が写真から大きくはみ出してもいた
- 除外されたランは表でも薄く描き、意味の欄が「除外」になる
- `tests/unit/annotateTable.test.ts` を追加（`src/annotate/` は
  カバレッジ対象外だがテストが 1 件も無かった）

### 金・銀が読めない原因を切り分けた（3 案とも効かず）

- `docs/11-金銀バンドが読めない理由.md` を追加。金・銀はたしかに検出できて
  いない（金を含む 35 枚のうち **31 枚が「ラン 3 本 / バンド 4 本」**で、
  不足はきっかり金 1 本）。**しかしそれは読み取りの律速ではない**
- 検出ラン数と正解バンド数の差で層別すると話が反転する。
  **「金バンドだけが欠けている」状態が唯一の成功パターン**で、
  27 枚すべてがこれ。許容差バンドは値に影響しないので無くても読める。
  つまり**金・銀の検出を直しても読み取りは 1 枚も増えない**
- 読めていない 12 枚は別の問題。**5 本帯が 0/7 で全滅**しており、しかも
  05 / 15 / 23 / 39 は本数が合っている（分割ではなく確信度の問題）。
  02 / 04 / 38 はラン 1〜2 本しか出ない重度の検出不足
- **効かなかった: 金属光沢（ラメ）での判別。** `docs/07` は分散 σ で失敗して
  いたので、尖度・外れ値率・高周波で測り直したが、標本 4 件では統計にならず
  本体と重なる。帯画像を目視しても金は**茶色っぽい平坦な縞**で輝きが無い
- **効かなかった: ROI 解像度の引き上げ。** `rectify` は入力の大きさによらず
  ROI を高さ 40px にリサンプルしており、入力を 800 → 2400px にしても何も変わらない。
  `targetHeight` 自体を上げると過分割で悪化し（40px の 27/0/12 に対し
  160px は 24/4/11）、金らしいランもほとんど増えない
- **効かなかった: 基準色の金を実測値に差し替える。** 実物の金が基準色より
  はるかに低彩度（C\* 59.6 に対し実測 27〜35）なのは事実だが、
  `[192,153,99]` に変えても読み取りは 27/0/12 のまま。
  分類の基準を直しても**分類すべきランが無い**
- 現行の `edgeΔE = 20` が金を潰しているという疑いも否定された
  （9 で 11 枚、20 で 9 枚と差が小さい）

### 対応付けでバンド側も飛ばせるようにした（較正での有効化は保留）

- `alignRunsToBands` の DP は**ランは飛ばせるがバンドは飛ばせなかった**
  （`runs.length < bands.length` で即 null、終端で全バンドの消費を要求）。
  金・銀の許容差バンドは本体色に溶けてランが出てこないので、存在しない
  バンドを必ずどれかのランに割り当ててしまう
- `maxBandSkips` を追加（**既定 0 で従来どおり**）。飛ばしたバンドは割り当てに
  含めないので、存在しない色を学習しない。1 本あたり固定コストを課す
  （無料だと「全バンドを飛ばす」が総コスト 0 の最適解になって退化する）
- **較正ではまだ有効にしない。** 1 本飛ばせるようにすると、白い机の誤学習は
  実際に止まる（金 17 件 L\*125 → 3 件 L\*56、対応付け 30 → 32/39、
  金が初めてパレットに入る）。ところが残った 3 件も**本体ベージュ寄り**
  （b\*22、基準は b\*59）で、今度は本体色として誤学習する。
  そのずれ 38 は誤学習ガード（60）を通ってしまい、**誤答が 0 → 1 件に増える**。
  ずれ 76 で弾かれていたのは偶然の安全弁だった
- `MAX_ALIGN_COST` を 25 → 28 にすると銀も初めて実測値を得る
  （L77 a-3 b20、基準 L78 とほぼ一致）が、2 件で採用下限 3 に届かず
  パレットには入らない。安全弁を緩める見返りが無いので 25 に戻した
- **金・銀を正しく学ぶには、先に `sample/labels.json` の人手ラベルが要る。**
  この 39 枚では金の使える標本が 3 件しかなく、データ側が足りていない

### 確信度の内訳を見えるようにした（効かなかった 2 案の記録つき）

- `JointReading` に `breakdown`（`meanDeltaE` / `absolute` / `margin` /
  `plausibility` / 次点との差と**次点が指す値**）を追加。
  `npx tsx scripts/dbgConfidence.ts` で 39 枚を一覧できる。
  確信度が低い理由を切り分けられないと式を直しようがないため
- **効かなかった: 確信度の式の再重み付け。**
  `05`（0.34）と `23`（0.34）は値も色も完全に正解なのに保留になる。
  当初は絶対項（`absolute = 1 - meanΔE/40`）が青ボディで底上げされた ΔE を
  過小評価しているためと見ていたが、**測ると absolute は 0.63〜0.71 で健全**、
  効いているのは **margin（0.14〜0.25）**だった。
  そして **39 枚すべてで次点は「別の抵抗値」**（05 は 7.5Ω 対 7.5MΩ、
  23 は 3.9kΩ 対 390Ω）。同値の僅差は 1 件も無い。
  つまり確信度が低いのは**正しい振る舞い**で、式を持ち上げるのは
  「390Ω かもしれないものを 3.9kΩ と自信ありげに出す」ことになる。採らない
- **効かなかった: `seriesRank` の E96 対応。**
  `seriesRank` は E6/E12/E24 しか見ず、E96 の正規値（2.26k など）が
  系列なし扱いで減点される。`snapForTolerance` は許容差から E96 を選ぶのに
  ここだけ見ていない、という不整合は事実。しかし直すと**逆効果**だった:
  E96 は 1 ディケードに 96 値と密なので、**誤読候補まで「系列に載る」ことに
  なって安くなり**、次点との差が縮む（`01` 0.34 → 0.28、`21` 0.23 → 0.17）。
  減点を緩めて中間を取っても baseline 復帰どまりで、正解数は終始変わらず。
  `snapForTolerance` の「格子を粗く保つことで誤読の棄却能力を維持する」が
  `seriesRank` にも当てはまる。**不整合はあえて残す**

### 自信ありげな誤答を 0 件にした

- ラン分割のしきい値を **edgeΔE 9 → 20 / clusterΔE 18 → 24 / minWidth 2** に変えた。
  `core/settings.ts` に集約し、`refineOptions` と `analyzeOptions` が共有する
- **正解 28・誤答 1 → 正解 27・誤答 0**（保留 10 → 12）。正解 1 枚と誤答 1 枚の交換。
  「誤った値を自信ありげに出さない」方針（設計メモ §2 [5]）を正解数より優先した
- edgeΔE 20 は clusterΔE 10〜30 の**全域で誤答 0** になる平坦域。edgeΔE 16 でも
  誤答 0 になる組はあるが clusterΔE 24 の一点だけで、隣が誤答 1〜2 に転ぶので採らない
- ラン 3 本に収まった写真が 23 → 27 枚に増え、**その 27 枚はすべて正解**
  （以前は 23 枚中 22 正解 1 誤答）
- **較正側（`calibrate.test.ts`）のしきい値はあえて揃えない。**
  揃えると対応付けの成立は 30 → 33/39 に増えるが、学習されるパレットが変わり
  読み取りが**誤答 2 件に悪化**する。較正は「取りこぼさない」、読み取りは
  「余計に割らない」で目的が逆。パレットと変換は密結合していて、
  較正の質が上がることは読み取りの質が上がることを意味しない

### 較正の計測を本番条件にそろえた（色判定改善の前提づくり）

- **フィクスチャ 3 本が本番と違う条件で測っていた**。`samples.test.ts` /
  `sweep.test.ts` / `diagnose.test.ts` が `ROI_PADDING = 0.06` を直書きし
  （本番は `settings.ts` で 0.28）、`refineBoxExtent` も `bodyRange` も
  `bodyLightnessWeight` も渡していなかった。`sample-report.txt` の
  「値一致 21/39」も `sweep-report.txt` の「最良 20/39」も本番の数字ではなく、
  **これを見て閾値を決めると誤誘導される**状態だった
- 本番経路（検出 → 枠の広げ直し → ROI → 解析 → 役割つきデコード）を
  `src/annotate/read.ts` に切り出し、GUI・バッチ・フィクスチャが同じ関数を
  通るようにした。`annotateImage` は焼き込みだけを担当する。
  **`doDetect.sh` の出力は 1 バイトも変わらない**ことを確認済み
- `samples.test.ts` は「値一致」だけでなく**実機での見え方（正解 / 誤答 /
  保留）**と**ラン数ごとの層別**を報告する。値一致 30/39、
  実機での見え方は **正解 28 / 誤答 1 / 保留 10** で `doDetect.sh` と一致
- `sweep.test.ts` は ROI 化までを本番条件で行い、掃引をラン分割のパラメータ
  だけに絞った。最良の選び方も「正解の最大」から**「誤答を増やさない中での
  正解の最大」**に変えた
  - 現行設定（edgeΔE 9 / clusterΔE 18 / minWidth 2）は 75 通り中で
    **正解が最大（28）**。ラン分割にはもう伸びしろが無い
  - ただし **誤答 0 で正解 27 の組み合わせが 3 つある**
    （edgeΔE 16〜20 / clusterΔE 14〜24 / minWidth 2）。
    正解 1 枚と誤答 1 枚を交換する選択肢として記録しておく
- `diagnose.test.ts` を**ラン単位**のダンプに作り替えた。以前は列ごとに
  `classifyBandColor` / `isBodyColor` を出していたが、**本番の主経路は
  「ラン → `rankBandColors`」で、列ごとの分類は本番が一度も計算しない量**
  だった。ラン 1 本ずつの Lab・LCh・上位 3 候補・採用された役割を出す
- `npm run recalibrate` で「較正 → 評価 → 診断」を一息に回せるようにした。
  変換・パレット・閾値は密結合していて、**片方だけを差し替える A/B は
  意味を持たない**（変換を 1 つ変えるだけで 28 → 25 に落ちる実測がある）

### 色空間と色差指標を検討した（結論: ΔE2000 を据え置き）

- `docs/09-色空間と色差指標の検討.md` を追加。**当初有力とみた HyAB への
  差し替えは実測で否定された**
- 基準色テーブルどうしでは HyAB が紛らわしいペアの余裕を 2〜3 倍に広げる
  （金/本体ベージュ ΔE00 13.7 → HyAB 42.3）が、**実写では現行より悪い**
  （正解ラベルのある 15 バンドで ΔE2000 10/15 に対し HyAB 9/15）。
  実写は基準色より軒並み低彩度で、彩度方向の差を強調すると誤差が増幅される。
  **問題は指標ではなくパレット**
- 併せて次の仮説も実測で否定した
  - **白飛びが色を壊している** → 本体窓内の飽和列は 38/39 枚で 0 件
  - **`L*>100` は飽和が原因** → 原因は `adaptToAnchor` の L\* 乗算のみ。
    `lightnessScale` は 39/39 枚で 1 超（1.10〜3.75）
  - **L\* を 100 でクランプすれば直る** → 誤答が 1 → 2 に増える。さらに金の
    誤学習ガード（ずれ 60.07 対 閾値 60）を無効化しかねない

### 「サンプルで試す」が読めなくなっていたのを直した

- 合成サンプルの抵抗器が画面を埋めていた（本体が 55%）。検出は「前景が
  35% を超えるのは机の質感を拾っている」とみなして閾値を上げ直すため、
  この保護が働いて**本体そのものが背景側に落ち**、彩度の高いバンド 4 本
  だけが前景として残っていた。縦長のバンドが抵抗器と誤認され、
  `90° / 65px` の箱になってバンドを 1 本も読めなかった
- 実写と同じく画面の 1 割強に収まるよう、抵抗器の周りに余白を取った
  （`FRAME_SCALE`）。`0° / 185px` で検出され、4.7kΩ ±5% と読める
- 画素の組み立てを `sampleImage.ts` に切り出して DOM 非依存にした。
  写真もカメラも無い相手が最初に押すボタンなので、**合成画像から値が出る
  までを通しでテストする**（`tests/sampleImage.test.ts`）。この経路は
  これまで一度も検証されておらず、それが今回の見逃しの理由

### GitHub Pages で公開できるようにした（doDeploy.sh）

- `./doDeploy.sh` を追加。検証 → パレット同期 → push → Actions の完了確認 →
  公開 URL の応答確認までを一本にまとめた。`--dry-run` で push 前に止められる
- ビルドとアップロードは GitHub Actions（`.github/workflows/pages.yml`）が行う。
  `dist/` は `.gitignore` 済みなので `gh-pages` ブランチは作らない
- Vite の `base` を **`'./'`（相対）** にした。既定の絶対パスだと
  プロジェクトページ（`/ohm-lens/` 配下）で最初のスクリプト読み込みから壊れる。
  相対にしておけばカスタムドメインへ移す際もビルド設定を触らずに済む
  - 巻き戻りに公開後まで気づけないので、`dist/index.html` に絶対パス参照が
    無いことを doDeploy.sh と pages.yml の両方で検査する
- 学習パレットを `src/debug/public/palette.json` に置き、公開版でも較正済みの
  基準色が効くようにした。非公開リポジトリ側との同期は doDeploy.sh が行う
- `fetch('/palette.json')` を `'./palette.json'` に変更（サブパス配下対策）
- CI にも `npm run build` を追加。これまで build は CI で一度も走っていなかった

### カラーコードに通し番号を振り、色空間の印を文字に変える

- カラーコードに **1 から通し番号**を振り、バンドのすぐ外側に焼き込む
- 色空間の印を記号から文字に変更
  - 基準色: 赤い `+` → **色名をその色で**（`白` `金` …）
  - 実測色: 赤い `○` → **通し番号をその色で**。写真の番号と突き合わせれば、
    どのバンドがどの基準色へ引き寄せられたかを追える
  - 明るい色は白地に埋もれるので、明度に応じてフチの色を入れ替える
- バンドを囲む枠を**塗らない**ようにした。半透明の白を敷いていたが、
  確認したい色帯そのものを薄めていた
- 番号・色名・意味を出す向きを**水平なら下、垂直なら右**に統一（`labelSide`）。
  従来は常に上側で、垂直の抵抗器では左に出て読みにくかった
- ブラウザのオーバーレイ（`detectionOverlay.ts`）も同じ扱いに揃えた

### 注釈を番号の直下へ、色空間に近傍色の一覧を追加

- 番号・色名・意味を**画像の軸に沿って**積むようにした（水平なら真下、垂直なら
  真右）。従来は法線方向に出していたので、傾いた抵抗器では斜めに流れて番号と
  色名が揃わなかった
- 注釈が写真からはみ出す量を先に測り（`labelOverflow`）、そのぶんキャンバスを
  広げてから色空間パネルを置く。**文字がパネルに重ならない**
- 平面図の下の一覧を**1 バンド 1 行**にし、近い基準色を 2 通り並べた
  - **面** — その平面の 2 軸で測った距離（面ごとに違う）
  - **空間** — その色空間の 3 軸で測った距離（RGB は 0〜1 のユークリッド、
    Lab は CIE76。同じ空間の 2 面では同じ）
- 平面図を**画像の幅に追従**させた（1 面が画像幅の半分ほど。800px の写真で
  1 面 344px、従来は固定 200px）。パネルの寸法は定数から関数に変えた
- 枠と目盛りを黒にした（格子は薄いまま。黒くすると点が埋もれる）

### 色空間を平面表示だけにする

- **立体表示（RGB 立方体・Lab 直方体）を廃止**。重なった点の前後関係が読めず、
  場所も食う。どの成分で近いのかは平面図のほうが速く分かる
- 平面図を 4 枚に絞って大きくした（一辺 200px）
  - 各軸に**目盛りと数値**を付けた（RGB は 0〜1、Lab は実寸）
  - 面の中に**格子**を引いた
  - 面の下に **`1 (0.34, 0.87) 2 (0.44, 0.58)`** と実測値を並べた
- 実測色の**番号を色名より手前**に描くようにした

### 確信度の %表示と、色空間の平面図

- 確信度を **%** で表示する（`formatConfidence`）。バッチの `summary.txt`、
  ブラウザの読み取り結果・バンド一覧のすべてを揃えた
- 色空間パネルに**2 次元の平面図**を追加。RGB は `B-R` と `G-B`、
  CIE L\*a\*b\* は `b*-a*` と `b*-L*`。立体図は散らばりを掴むのに向くが
  重なった点の前後関係が読めないので、指定の 2 軸だけを見る平面図を並べる
  （軸は先に書いたほうが横）
- パネルは 2 行になった（1 行目 RGB、2 行目 Lab。各行は立体 1 面 + 平面 2 面）

### 自信ありげな誤答を 8 件 → 1 件に

- 確信度の閾値を 0.5 → **0.4** に。実測すると 0.5 は正解を 1 枚余分に捨てるだけで
  誤答は減らせていなかった
- 次点との差を「コストの合計に対する割合」ではなく**固定の尺度**で測るように
  直した。系列や流通範囲の事前分布を足したぶん分母が膨らみ、同じ差でも
  確信度が下がる副作用があった
- 金属色に最も近いランを数字として読み替えた解釈は、本数 1 本につき確信度を
  半分にする。IEC 60062 では金属色は倍率と許容差にしか現れない。閾値を下げると
  `gold silver gold` のような物理的にありえない列まで値として出ていた
- `doDetect.sh` が**実機での見え方**（正解／誤答／保留）を出すようにした。
  「値一致」だけでは自信ありげな誤答が何件あるか分からない。
  実測: 正解 12 / 誤答 1 / 保留 26 → **正解 28 / 誤答 1 / 保留 10**
- 色空間パネルが写真より広いとき、キャンバスを右にも広げるようにした
  （400px 幅の画像で Lab 面が切れていた）
- `summary.txt` に確信度を出すようにした（`[確信度 0.35]`）。保留の理由を
  追うのに毎回使い捨てのスクリプトを書いていた

### 金・銀バンドの検出

- `identifyBody` に「本体色の連なりの中の明度の凹凸」判定を追加。金・銀は
  本体と**色相が同じで明度だけ違う**ため、円筒の陰影対策で入れた明度の重み
  0.6 がそのまま飲み込んでいた（02 では本体 L68 a13 b22 に対し金 L51 a14 b20）
  - 陰影は端へ向かって単調に変わり、バンドは局所的に凹む。前後の両方より
    12 以上明度が違うランには明度の緩和を効かせない
  - **前後が本体色であること**を条件にする。これが無いと色バンドに挟まれた
    本体（茶・本体・茶）まで外してしまう
  - 効かなかった見分け方: 金属光沢の分散（ラン内 L\* の σ が金 8.5〜13.9・
    本体 6.1〜18.9 で重なる）、細いランの除外（29 → 26 枚に悪化）
- 02-1.5ohm が `brown1 green5 gold×0.1 grey±0.05%` と金を拾い、正解に
- 39 枚での実測: 値の一致 29/39 → **30/39（77%）**

### 検出結果画像に色空間を描く

- `src/annotate/colorSpace.ts` を追加。結果画像の左下に **RGB** と
  **CIE L\*a\*b\*** の色空間を描き、基準色を赤い `+`、その写真で検出した
  バンドの実測色を赤い `○` で置く。誤読を追うとき、色名だけでは「惜しかったのか、
  まるで違うのか」が分からなかった
- **どちらも 3 軸のまま斜投影で描く。** a\*b\* 平面だけだと黒・灰・白・銀が
  すべて原点に重なり、いちばん取り違えやすい組が見えなくなる。真の等角投影は
  灰色軸に沿って見るので黒と白が重なるため、斜投影にしている
- 画像は帯のぶん下に伸ばす（`sharp.extend`）。写真の上に重ねると抵抗器が隠れる

### 実物の分布を使って二択を決める

- `seriesRank` を追加し、同時デコードのコストに**系列の一般性**を入れた。
  E6 ⊂ E12 ⊂ E24 で市場に出回る数がまるで違う。色が紛らわしくて二通りに
  読める（1MΩ と 1.1MΩ、220Ω と 1.2kΩ、1kΩ と 2kΩ）とき、どちらも E24 に
  載るのでスナップ偏差では差がつかなかった
- `decadesOutsideCommonRange` を追加し、**流通範囲（1Ω〜10MΩ）から外れた値**を
  桁数に応じて減点する。誤読は「白を倍率と読んで ×10^9」のように桁が跳ねる
  形で出ており、390GΩ・82GΩ・33GΩ といった出力が消えた
- 39 枚での実測: 値の一致 25/39 → **29/39（74%）**。
  **3 ランに収まった 28 枚のうち 27 枚（96%）が正解**
- 詳細は [docs/07-色帯読み取りの改善.md](docs/07-色帯読み取りの改善.md)

### 断面プロファイルで検出枠を伸ばす（07-10ohm-on-carpet の解決）

- `refineBoxExtent` を追加。**判断材料を画素マスクから断面の中央値
  プロファイルに変える**。`extractProfile` は ROI の中央 50% の行の中央値を
  取るのでカーペットのざらつきが平均化され、画素単位では背景差が 10〜27 と
  ばらつく領域も、断面で見れば赤いボディ（a\* 25〜39）・カーペット（5〜12）・
  金属リード線（−3〜7）にはっきり分かれる
  - 背景 → 本体の a\*b\* 方向への射影で「本体らしさ」を判定し、軸に沿って
    外へ歩く。決め手は ΔE でも L\* でもなく色度
  - 途切れが太さの 0.4 倍より長く続いたら止まる（バンドは 0.2 倍ほど、
    リード線は途切れずに続く）
  - 細長さ 1.8〜4.5 の範囲、かつバンドが 3〜7 本になることを検算する
  - 細長さ 2.4 以上の枠には手を出さない
- **07 は比 1.75 → 3.69（L158 → L333、実寸 ~354）**。本体全体を囲むように
  なった。ブラウザでも 433px → 799px
- **39 枚すべての長さ:太さ比が 2.59〜3.69 に収まった**（中央値 2.87、
  以前は 1.84〜3.96）。軸形抵抗器の実寸と整合する
- 等間隔性の採点（`bandLayoutScore`）は実測で二度とも逆効果だったため削除し、
  本数の規格だけを `hasPlausibleBandCount` として残した。枠を伸ばす基準に
  すると本体の一部しか覆っていない枠が背景の縞を拾って 0.80・正しい枠が
  0.29、検算に使うと 07 の正しい枠を弾いた
- 値の一致は 25/39 で変わらず（枠は正しくなったので残りは色の読み取り側）
- 解析条件を `src/core/settings.ts` に集約。GUI だけ ROI 余白 0.06 の
  古い設定のまま取り残されており、バッチで測った値が実機に反映されて
  いなかった。GUI も検出枠の事前情報・明度の重み・枠の広げ直しを使う

### マスクが分断される写真の検出（01, 03, 07）

- **近くにある成分をまとめる**（`groupComponents`）。膨張したマスクで連結を
  取り、元の画素をそのまとまりに集める。本体の一部が背景と同じ色だと
  マスクが島に割れ、一番大きな島だけでは本体の一部しか囲めなかった
- **本体範囲の切り方を 3 通り試して採点の最も高いものを採る**。
  「最大の太さの 45% 以上」は 1 通りに決まらず、緩めると隣の塊まで拾う画像と、
  緩めないとバンドの間で切れる画像が両方あった
- **太さと中心線を太い断面から取る**。中央値だと地の色が抜けた薄い断面に
  引きずられ、太さが 3 割痩せて中心もずれていた
- **途切れを跨ぐ条件を整理**。中心線の一致は地続きでも判定し（抵抗器の
  中心線はまっすぐ）、太さの一致は途切れを跨ぐときだけ見る（本体はバンドの
  間で細くなりうる）。比べる相手は直前ではなく区間で最も太い断面
- 01 は比 1.97 → 2.81、03 は L99 → L187（比 2.59）に改善。値の一致 24 → 25/39
- **07-10ohm-on-carpet は未解決**。本体の左半分が前景マスクに出ない。
  局所背景・閉処理・ΔE 引き下げ・粗さによる分離をいずれも試して効果なし
  （[docs/06](docs/06-検出精度の改善.md) に記録）

### 色帯読み取りの精度改善

- 本体が ROI のどの列を占めるかを**検出結果から計算**して `analyzeRoi` に渡す
  （`bodyColumns` / `AnalyzeOptions.bodyRange`）。プロファイルからの推定は
  39 枚中 25 枚で外れており（広すぎ 10・狭すぎ 15）、広すぎれば背景がバンドに、
  狭すぎれば端のバンドが消えていた。ROI 端に達する誤りは 10/39 → 0/39
- `identifyBody` の本体クラスタで**明度の重みを下げる**（`bodyLightnessWeight`）。
  円筒の陰影で本体の L* は中央値 15・最大 28 ばらつき、陰の部分がバンドとして
  数えられていた
- `identifyBody` を**代表色での取り込み直し**に変更。貪欲クラスタリングは
  クラスタの先頭と比べるため、明るい端から暗い端へ段階的に変わる本体を
  取りこぼし、結果が並び順に依存していた
- `paletteOverrides` に**既定値からかけ離れた学習を捨てるガード**を追加。
  金が「L126 のほぼ白」として 15 件学習されており、4 本帯の許容差バンドを
  壊していた。較正フィクスチャも同じ関数を通し、GUI の学習と判定を統一
- 解析条件を `analyzeOptions()` に集約し、デバッグツールと本番のずれを防ぐ
- ラン分割を見る `scripts/dbgRuns.ts` を追加（ラン別の Lab・幅・本体色との差、
  ROI とラン境界を重ねた帯画像）
- `isBackgroundLike` の照明判定を**明暗どちらにも効かせる**。影（暗い側）だけを
  背景に含めていたため、光源側が明るい写真ではその領域が巨大な前景の塊になり、
  23-3.9kohm では検出枠が写真の左上に出ていた
- **解析範囲の端に接するランをバンドにしない**。バンドの外側には必ず地の色が
  あるので、端から始まるランは本体の肩の照り返しか背景。失敗の多くが先頭に
  「白」を出していた原因。ただし落とした結果 3 本を切るなら落とさない
- **隣り合う同色のランをまとめる**（`mergeDeltaE`）。太いバンドは中の濃淡で
  2 本に割れ、本数が合わなくなっていた
- 較正フィクスチャを `bandRuns` に寄せて解析側と経路を統一。ただし端のランは
  残す（`keepEdgeRuns`）— 余分なランは DP が飛ばせるが、落としたランは戻せない
- 39 枚での実測: 値の一致 16/39 → **24/39（62%）**、3 ランに収まった画像
  17 → 27、7 ラン以上 13 → 0（最大 6 ラン）、較正での対応付け成立 21/39 → 31/39
- 詳細は [docs/07-色帯読み取りの改善.md](docs/07-色帯読み取りの改善.md)

### 抵抗器検出（赤い四角形）の精度改善

- 影を背景に含める（`isBackgroundLike`）。色相が背景と同じで明度が少し
  下がっただけの画素は前景にしない。机や布の上で撮ると影が本体と地続きに
  なり、太さを 1.5〜2 倍に見積もっていた
- 前景が 35% を超えたら ΔE の閾値を上げて作り直す（最大 4 回）
- 前景マスクに開処理（収縮 → 膨張）を入れ、リード線・カーペットの繊維・
  スキャン画像の枠線を本体から切り離す
- ビンごとの太さを「重心からの距離 × 2」ではなく**断面の上下端の差**で測る。
  あわせて本体の中心線を求め、ボックスの中心をそこに合わせる
- 本体の途中の途切れを跨げるようにした。ただし跨ぎ先の中心線と太さが揃って
  いるときだけ（白背景の白バンドで穴が開くのに対応しつつ、別の塊まで
  繋がないため）
- 連結成分を**最大のもの**ではなく**最も抵抗器らしいもの**で選ぶ
  （細長さ 1.65〜14、面積比 0.4〜55%、回転矩形の充填率で採点）
- ROI の余白を 0.06 → 0.28 に。検出が本体に張り付いた結果、端のバンドが
  半分欠けるようになったため（較正込みの掃引で 0.22〜0.32 が平坦、その中央）
- 検出だけを詳しく見る `scripts/dbgLocate.ts` を追加（前景マスクの PNG 出力、
  候補ごとの採点と却下理由）
- 39 枚での実測: 長さ:太さ比の中央値 2.38 → 3.06、最小 1.20 → 1.79、
  検出成功 36/39 → 39/39、値の一致 14/39 → 16/39
- 詳細は [docs/06-検出精度の改善.md](docs/06-検出精度の改善.md)

### Phase 0 実装（Step 0-1〜0-6）

- Vite + TypeScript + Vitest + ESLint のスキャフォールドと GitHub Actions CI
- `core/` に色帯解析パイプラインを実装（DOM 非依存、Worker でそのまま使える）
  - `color/` — sRGB↔Lab65 変換、ΔE2000、基準色テーブル、本体色アンカーによる
    色順応補正
  - `bands/` — 1D カラープロファイル抽出（列ごとの中央値）、本体色除去と
    ランのラベリング、Lab + ΔE2000 分類
  - `value/` — IEC 60062 カラーコード表、E24/E96 スナップ、読み取り方向判定、
    バンド列デコード
- 静止画デバッグページ（`src/debug/`）— 各段階の可視化と合成サンプル生成
- 確信度が閾値未満のときは値を出さず「?」を表示する（設計メモ §2 [5]）
- テスト 215 件、カバレッジ 99.6%

### セットアップ

- リポジトリ初期化（README / CLAUDE.md / ディレクトリ構成）
- MIT ライセンスを追加
- 設計メモ `docs/01-設計.md` を取り込み、README / CLAUDE.md を OhmLens の
  実内容（ブラウザ完結の Web アプリ）に合わせて更新
- 実装計画 `docs/02-実装計画.md` を追加（Phase 0 の TDD タスク分解、
  リポジトリ構成、出口条件、リスク）
- アーキテクチャ・ライブラリ検証レビュー（2026-07-27）を設計メモに反映：
  WB ロック非対応環境（Safari）向けの相対色分類、OpenCV.js の Phase 0 限定化、
  YOLO26 NMS-free、E系列スナップ順序、WebGPU Baseline 到達
