# Changelog

このプロジェクトの変更履歴。

## [Unreleased]

### 抵抗器検出（赤い四角形）の精度改善

- 影を背景に含める（`isBackgroundLike`）。色相が背景と同じで明度が少し
  下がっただけの画素は前景にしない。机や布の上で撮ると影が本体と地続きに
  なり、太さを 1.5〜2 倍に見積もっていた
- 前景が 35% を超えたら ΔE の閾値を上げて作り直す（最大 4 回）
- 前景マスクに開処理（収縮 → 膨張）を入れ、リード線・カーペットの繊維・
  スキャン画像の枠線を本体から切り離す
- ビンごとの太さを「重心からの距離 × 2」ではなく**断面の上下端の差**で測る。
  あわせて本体の中心線を求め、ボックスの中心をそこに合わせる
- 本体の途中の途切れを跨げるようにした。ただし跨ぎ先の中心線と太さが揃って
  いるときだけ（白背景の白バンドで穴が開くのに対応しつつ、別の塊まで
  繋がないため）
- 連結成分を**最大のもの**ではなく**最も抵抗器らしいもの**で選ぶ
  （細長さ 1.65〜14、面積比 0.4〜55%、回転矩形の充填率で採点）
- ROI の余白を 0.06 → 0.28 に。検出が本体に張り付いた結果、端のバンドが
  半分欠けるようになったため（較正込みの掃引で 0.22〜0.32 が平坦、その中央）
- 検出だけを詳しく見る `scripts/dbgLocate.ts` を追加（前景マスクの PNG 出力、
  候補ごとの採点と却下理由）
- 39 枚での実測: 長さ:太さ比の中央値 2.38 → 3.06、最小 1.20 → 1.79、
  検出成功 36/39 → 39/39、値の一致 14/39 → 16/39
- 詳細は [docs/06-検出精度の改善.md](docs/06-検出精度の改善.md)

### Phase 0 実装（Step 0-1〜0-6）

- Vite + TypeScript + Vitest + ESLint のスキャフォールドと GitHub Actions CI
- `core/` に色帯解析パイプラインを実装（DOM 非依存、Worker でそのまま使える）
  - `color/` — sRGB↔Lab65 変換、ΔE2000、基準色テーブル、本体色アンカーによる
    色順応補正
  - `bands/` — 1D カラープロファイル抽出（列ごとの中央値）、本体色除去と
    ランのラベリング、Lab + ΔE2000 分類
  - `value/` — IEC 60062 カラーコード表、E24/E96 スナップ、読み取り方向判定、
    バンド列デコード
- 静止画デバッグページ（`src/debug/`）— 各段階の可視化と合成サンプル生成
- 確信度が閾値未満のときは値を出さず「?」を表示する（設計メモ §2 [5]）
- テスト 215 件、カバレッジ 99.6%

### セットアップ

- リポジトリ初期化（README / CLAUDE.md / ディレクトリ構成）
- MIT ライセンスを追加
- 設計メモ `docs/01-設計.md` を取り込み、README / CLAUDE.md を OhmLens の
  実内容（ブラウザ完結の Web アプリ）に合わせて更新
- 実装計画 `docs/02-実装計画.md` を追加（Phase 0 の TDD タスク分解、
  リポジトリ構成、出口条件、リスク）
- アーキテクチャ・ライブラリ検証レビュー（2026-07-27）を設計メモに反映：
  WB ロック非対応環境（Safari）向けの相対色分類、OpenCV.js の Phase 0 限定化、
  YOLO26 NMS-free、E系列スナップ順序、WebGPU Baseline 到達
